services:
  defrustrator:
    build: .
    ports:
      - "8000:8000"
    environment:
      PORT: "8000"
      DEFRUSTRATOR_HARDWARE_PARTS: /data/hardware-parts.json
    volumes:
      - defrustrator-data:/data

volumes:
  defrustrator-data:
